version: 1.0.{build}
configuration: Release
init:
- cmd: git config --global core.autocrlf true
nuget:
  account_feed: true
  project_feed: true
before_build:
- cmd: nuget restore
build:
  publish_nuget: true
  verbosity: minimal